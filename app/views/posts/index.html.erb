<h1>投稿一覧</h1>


<% if user_signed_in? %>
  <%= button_to "投稿", {controller: 'posts', action: 'new'}, {method: :get} %>
<% end %>


<% @posts.each do |post| %>
  <span>投稿者：</span><%= post.user.name %>
  <%= post.title %>
  <%= post.post_content %>
<% end %>
